# crous

`crous` has moved to `surp`.

Install the maintained package with:

```bash
pip install surp
```

This compatibility package is published only for version `1.1.3` and emits a
deprecation warning before re-exporting `surp`.
